h1 = @question.body

h2 = t('.answers')
= link_to t('.new_answer'), new_admin_question_answer_path(@question)

ol
  = render @question.answers

= link_to t('actions.edit'), edit_admin_question_path(@question)
|  | 
= link_to t('actions.delete'), admin_question_path, method: :delete, data: { confirm: t('actions.sure?') }

br

= link_to t('actions.back'), admin_test_path(@question.test)
